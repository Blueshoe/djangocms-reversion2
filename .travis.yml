language: python
sudo: false

branches: master

install:
  - pip install -r test_app/requirements.txt


script:
  - env
  - ls -la
  - ls
  - PYTHONPATH=$PYTHONPATH:/home/travis/build/Blueshoe/ python test_app/manage.py test